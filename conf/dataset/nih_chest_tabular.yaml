defaults:
  - commons
  - _self_

name: nih_chest_tabular
loader:
    _target_: src.data.datasets.NIH_chest.NIHChestDataset
    modality: tabular
    val_size: 0.1
    ftune_size: 0.      # 0.3
    ftune_val_size: 0.  # 0.1

batch_size: 64

is_image_dataset: no
reduce_fraction: Null

load_embeddings: true
